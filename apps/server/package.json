{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --env-file ../../.env --hot src/dev.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@dealort/api": "workspace:*",
    "@dealort/auth": "workspace:*",
    "@dealort/db": "workspace:*",
    "@dealort/utils": "workspace:*",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "arcjet": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.44.2",
    "hono": "catalog:",
    "uploadthing": "^7.7.4",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/bun": "^1.2.6",
    "tsdown": "catalog:"
  }
}
